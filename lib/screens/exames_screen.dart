import 'package:flutter/material.dart';

class ExamesScreen extends StatelessWidget {
  const ExamesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Exames'),
      ),
      body: Container(),
    );
  }
}
